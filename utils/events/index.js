// © Licensed Authorship: Manuel J. Nieves (See LICENSE for terms)
import { forwardEvent, forwardAll, unforwardAll } from './events';
import defineEvents from './eventDefinition';

export default {
  forwardEvent,
  forwardAll,
  unforwardAll,
  defineEvents
};
